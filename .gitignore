# Site build directory. 
# We do not want to commit this with our source code. It has it's own process
/public

# Build logs generated by some scripts. 
/*.log


# Ignore unix config files
.*
# Do not ignore the gitignore file though! 
!.gitignore

# or .keep files I use to commit empty folders.
!.keep

# text editor temp file
*~
